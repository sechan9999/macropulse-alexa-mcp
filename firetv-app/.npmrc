# react-native-tvos publishes as "0.74.2-0", which fails some packages' semver peer ranges
# although it is ABI-compatible. EAS Build runs a plain `npm install`, so the flag must live here.
legacy-peer-deps=true
